import TitleDescription from "../../../Shared/TitleDescription";

const Calendar = () => {
  return (
    <div>
      <TitleDescription title={"Calender"}  description={"Provide a calendar view for task Scheduling content would be like show a calendar interface where users can view task scheduled for specific dates set due dates or create task directly from calendar interaction users can manage their task visually by setting due dates and to viewing task schedules"} />
    </div>
  );
};

export default Calendar;
