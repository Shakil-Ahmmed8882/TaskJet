import TitleDescription from "../../../Shared/TitleDescription";

const MyDashboard = () => {
  return (
    <div>
      <TitleDescription title={"My dashboard"} description={"Display summary statistics:Total number of tasks.Number of tasks in each category (To Do, Ongoing, Completed).Recent activity or updates related to tasks.Graphical representation (optional) for visualizing task distribution.User's Experience: When users land on the Dashboard, they get a quick glance at their tasks. They can see how many tasks they have, which tasks are pending, ongoing, or completed, and any recent updates, allowing them to plan their work effectively."} />
    </div>
  );
};

export default MyDashboard;
