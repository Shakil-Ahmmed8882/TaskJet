import TitleDescription from "../../../Shared/TitleDescription";


const Setting = () => {
      return (
            <div>
                <TitleDescription title={"Setting"}   description={"Allow users to modify account settings preferences notifications or things like light and dark."}/>   
            </div>
      );
};

export default Setting;