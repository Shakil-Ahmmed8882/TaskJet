import TitleDescription from "../../../Shared/TitleDescription";


const Template = () => {
      return (
            <div>
                <TitleDescription title={"Template"}   description={"Provides predefined task templates for task lists for commonly recurring task or project types saving time on task creation example offers templates for routine meetings project kickoffs or specific workflow allowing users to start new task from predefined templates"}/>   
            </div>
      );
};

export default Template;