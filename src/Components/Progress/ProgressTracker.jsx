import TitleDescription from "../../Shared/TitleDescription";


const ProgressTracker = () => {
      return (
            <div>
                <TitleDescription title={"Progress tracker"}   description={"Tracks and visualizes the progress of ongoing task or projects providing insights into the task completion status example offers a visual representation like a progress bar starts or percentage to indicate task completions aiding in monitoring project milestones"}/>  
            </div>
      );
};

export default ProgressTracker;