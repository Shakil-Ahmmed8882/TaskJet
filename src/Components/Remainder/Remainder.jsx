import TitleDescription from "../../Shared/TitleDescription";


const Remainder = () => {
      return (
            <div>
                <TitleDescription title={"Remainders"}   description={"A dedicated area for managing and customizing task reminders including options for setting editing or deleting reminders."}/>  
            </div>
      );
};

export default Remainder;